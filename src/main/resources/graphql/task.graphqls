type Task {
    id: ID!
    name: String
    description: String,
    sequence: Int,
    section: Section
}

input TaskRequest {
    id: ID
    name: String
    description: String,
    sequence: Int,
    section: SectionRequest
}
type Task {
    id: ID!
    name: String
    description: String,
    sequence: Float,
    section: Section
}

input TaskRequest {
    id: ID
    name: String
    description: String,
    sequence: Float,
    section: SectionRequest
}
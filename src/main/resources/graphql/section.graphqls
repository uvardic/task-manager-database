type Section {
    id: ID!
    name: String,
    sequence: Int,
    project: Project
    tasks: [Task]
}

input SectionRequest {
    id: ID
    name: String
    sequence: Int,
    project: ProjectRequest
    tasks: [TaskRequest]
}
type Section {
    id: ID!
    name: String,
    sequence: Float,
    project: Project
    tasks: [Task]
}

input SectionRequest {
    id: ID
    name: String
    sequence: Float,
    project: ProjectRequest
    tasks: [TaskRequest]
}